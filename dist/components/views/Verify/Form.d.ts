import React from 'react';
type Args = {
    apiRoute: string;
    back?: string;
    length?: number;
    serverURL: string;
};
export default function OTPForm({ apiRoute, back, length, serverURL }: Args): React.JSX.Element;
export {};
