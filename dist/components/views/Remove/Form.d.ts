import React from 'react';
type Args = {
    apiRoute: string;
    length?: number;
    serverURL: string;
};
export default function OTPForm({ apiRoute, length, serverURL }: Args): React.JSX.Element;
export {};
